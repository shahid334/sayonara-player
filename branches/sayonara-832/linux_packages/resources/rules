#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk

build-arch:
	cmake . -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_VERBOSE_MAKEFILE=ON
	make

common-install-arch::
	make install DESTDIR=$(CURDIR)/debian/sayonara

clean::
	rm -rf $(CURDIR)/build

